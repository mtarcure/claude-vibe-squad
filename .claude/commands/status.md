# /status

Show current Vibe Squad state from inside Chrono.

Run:

```bash
bash ~/Obsidian-Claude-Vibe-Squad/bin/where-are-we.sh
```

Use this when the operator says `/status`, "where are we", or asks what is active.
