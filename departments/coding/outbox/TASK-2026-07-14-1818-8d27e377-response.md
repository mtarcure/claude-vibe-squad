---
id: TASK-2026-07-14-1818-8d27e377-response
in_response_to: TASK-2026-07-14-1818-8d27e377
from: gpt-codex
to: chrono
to_lead: chrono
type: RESULT
status: needs_review
specialist: ai-engineer
review_model: claude
mandatory_review: true
capability_gap: none
---

# Phase 3 closeout — capture policy and pre-commit leak guard

## Outcome

Implementation and tests are complete. The registered `ai_engineer` adapter was used; there is no specialist capability gap.

Part A took the task-approved fail-closed option. `capture_session`, `capture_dispatch`, `capture_review`, and `capture_research` retain their existing signatures but no longer import or invoke the separate capture writer. Each now returns:

```json
{"ok": false, "disabled": true, "error": "capture_disabled_pending_canonical_migration"}
```

This ensures those endpoints cannot write prompts, outputs, or other capture data outside the canonical private-root policy while migration is pending.

Part B adds an executable tracked hook at `scripts/hooks/pre-commit`. It reads staged Git index blobs and rejects:

- `sensitivity: restricted` in YAML frontmatter;
- paths under `_state/bounty/`;
- `kg.db*`, `*.db-wal`, and `*.db-shm` artifacts;
- paths containing the literal `${CHRONO_VAULT_ROOT}` component.

The hook reports each path and reason, exits non-zero on violations, and fails closed when staged objects cannot be inspected. It has no obsolete `send-task.sh` auto-snapshot requirement. `.gitignore` now explicitly carries the bounty, graph database, database sidecar, and existing phantom-root defenses.

## TDD evidence

RED:

- Capture policy test failed because `_load_capture_support` remained present and active.
- Hook tests failed because `scripts/hooks/pre-commit` did not exist and the requested explicit `.gitignore` patterns were absent.

GREEN:

```text
PYTHONWARNINGS=error python3 -m unittest discover -s plugins/chrono-vault/tests -p 'test_*.py'
Ran 78 tests — OK

python3 -m unittest discover -s tests/hooks -p 'test_*.py' -v
Ran 5 tests — OK

python3 -m py_compile plugins/chrono-vault/mcp_server.py scripts/hooks/pre-commit \
  plugins/chrono-vault/tests/test_capture_policy.py tests/hooks/test_pre_commit.py
OK

git diff --check
OK
```

Tests use temporary Git repositories and temporary vault roots only. They verify disabled capture tools perform no writes, prohibited staged content is rejected, normal content is allowed, staged blobs are inspected instead of unstaged worktree content, and a `send-task.sh` edit without auto-snapshot text is allowed.

## Scoped diff

Implementation/test diff before this response: 5 files, 360 insertions, 41 deletions.

- `.gitignore`: explicit private-memory/database ignores.
- `plugins/chrono-vault/mcp_server.py`: removed legacy capture support loading and disabled four unsafe tools.
- `plugins/chrono-vault/tests/test_capture_policy.py`: signature, stable response, no-write, and no-secret-echo coverage.
- `scripts/hooks/pre-commit`: tracked index-aware leak guard and install note.
- `tests/hooks/test_pre_commit.py`: restricted, database, bounty, phantom-root, normal-file, staged-blob, and obsolete-auto-snapshot regression coverage.

No files were deleted.

## Required handoff

The packet grants `.git/hooks/pre-commit` read context but not write scope, so the existing installed hook was not modified. Install the tracked replacement from the repository root after review:

```bash
ln -sf ../../scripts/hooks/pre-commit .git/hooks/pre-commit
```

Status remains `needs_review` because the packet requires Claude review. Runtime enforcement also depends on the above operator installation step; this response does not claim the tracked hook is already installed.
