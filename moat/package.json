{
  "name": "@vibe-squad/moat-boundary",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check:index": "node pipeline/check-invariant-index.mjs --index invariants/index.json fixtures/schema/valid/invariant.invariant.json",
    "test": "node --test tests/*.test.mjs",
    "tier-a": "node boundary/tier-a.mjs",
    "tier-b": "node boundary/tier-b.mjs"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
