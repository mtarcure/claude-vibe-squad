---
id: research/learning-study
mode: research
title: Learning + study (study plans · drills · learning paths)
capability_state: live
state_reason: Study-plan / drill / learning-path design is judgment-driven authoring by `learning-coach` + `summarizer`, backed by `chrono-vault` (all·yes) for durable progress notes. No catalog-absent tool is load-bearing; optional source material comes from the live research tools.
state_evidence: registry rows — chrono-vault = `all·yes·subscription`; arxiv_search = `all·yes·subscription`; perplexity_search_web = `claude·lane-live·metered` (optional source-gathering only). No catalog-absent tool on the path.
overlays: [review, memory]
gates: []
cost_note: core subscription-only — `learning-coach`/`summarizer` are lane-native judgment + `chrono-vault`; optional source gathering via `perplexity_search_web` is `metered` (budget-guarded) and not required for the core deliverable.
---

**When to use:** build a study plan, drill set, or learning path for a topic or skill. Judgment-driven; the
research tools are an optional aid for sourcing material, not a core dependency.

| Step | Specialists | Tools `(lane · state · cost_tier)` | Skills `(type)` | Gate / Overlay |
|---|---|---|---|---|
| **S0** Intake/Admit | `Chrono`, `triage` | `chrono-vault` (all · yes · subscription) | — | memory overlay (recall) |
| **S1** Frame (goals + level) | `learning-coach` | — | `scope-decomposition` (stub) | — |
| **S3** Produce (plan + drills + path) | `learning-coach`, `summarizer` | `chrono-vault` (all · yes · subscription) | `scope-decomposition` (stub) | — |
| **S4** Verify (coverage + difficulty fit) | `learning-coach`, `skeptic` | — | — | review overlay if the plan is high-stakes (certification/exam) |
| **S6** Ship/Deliver (study plan) | `learning-coach`, `technical-writer` | `chrono-obsidian` (all · yes · subscription) | — | — |
| **S7** Capture | `Chrono`, `memory-curator` | `chrono-vault` (all · yes · subscription) | — | memory overlay (record; progress tracking) |

**Notes.** Steps collapse (no S2/S5) — this is light, judgment-driven work. The core needs no catalog-absent
tool; `perplexity_search_web`/`arxiv_search` are optional live aids for sourcing reference material (metered
tools are budget-guarded). Durable progress notes are recorded to the vault at S7.
