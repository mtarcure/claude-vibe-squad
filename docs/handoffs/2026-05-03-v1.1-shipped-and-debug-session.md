# Handoff — v1.1 Shipped + Debug Session State

**Date:** 2026-05-03
**Status:** v1.1.1 shipped; tmux debugging in progress; Matt frustrated by my over-engineering pattern

---

## What's shipped + tagged on remote (github.com/mtarcure/claude-vibe-squad)

| Tag | Commit | What |
|-----|--------|------|
| `v1.0-pre-1.1` | 1004787 | Rollback baseline |
| `v1.1.0` | 0c5c596 | Initial v1.1 ship — 24 tasks, tool-utilization fixes |
| `v1.1.1` | d8e22c1 | 6 reviewer-flagged bug fixes (validate-specialists.sh awk range, dream_light redaction, doctor.sh retry-storm, vibecoding-check missing project-mode checks, CVSS validation, etc.) |

Latest commit: `a0110df` — "Roll back v1.1 tmux config — operator authorized rollback to defaults"

`main` and `origin/main` are in sync at `a0110df`.

## What v1.1 actually delivered (recap)

- 39 specialist files upgraded with v1.1 schema (Tools / fan-out / escalate / what-not)
- Per-pane effort tier defaults in launch-squad.sh
- Capability inventory (`_state/capability-inventory-2026-05-02.md`)
- Critical operational fix: chrono-* MCPs back online on Claude panes (tilde-path bug; see `_state/incident-2026-05-03-claude-mcp-tilde.md`)
- Topology B chaser logic in chrono/CLAUDE.md + chrono/current.md
- 4 new log streams (specialist-log, tool-calls, errors, patterns)
- 2 new skills (smart-contract-audit-checklist, bounty-platform-report-format)
- v1.1.1 reviewer fixes (validate-specialists.sh, dream_light.py, doctor.sh, vibecoding_check.py, CVSS validation)

## Today's debug journey (where I went wrong)

**Original problems Matt reported:**
1. tmux navigation broken — Ctrl-b + numbers didn't work
2. Chrono not dispatching to Leads — does work itself
3. Chrono using basic tools like WebFetch instead of dispatching

**What I did wrong:**
- Auto-enabled sidebar in `connect.sh` — caused operator to see 6 panes (sidebar split) and panic
- Wrote `~/.tmux.conf` with F1 panic key, Alt+number bindings, mouse mode, prefix-highlight — without verifying these work on Apple Terminal.app
  - Apple Terminal does NOT send Option-as-Meta by default → Alt+number bindings broken from the start
  - macOS captures F1-F4 for Mission Control by default → F1 panic key likely broken
- Killed tmux server during rollback, then re-launched without preserving launch-squad.sh's session-level setup (mouse on, scrollback 50K, status bar branding, prefix-Space panic key) — left session in WORSE state than v1.0 baseline
- Kept proposing more tmux UX layers (slash commands, MCP wrappers, Zellij migration) when Matt's actual point was: **navigation should not exist for the operator at all**

**What I rolled back:**
- `~/.tmux.conf` → moved to `~/.tmux.conf.v1.1-rolled-back` (kept for inspection, not active)
- `tmux-config/` directory in vault → deleted
- Commit `a0110df` documents the rollback

**What evidence I gathered:**
- Apple Terminal.app is the operator's terminal (TERM_PROGRAM=Apple_Terminal)
- launch-squad.sh ECHOES start commands instead of running them — pane capture confirms `chrono@Chronos-Mac-mini chrono %` bash prompts with `echo 'Start with: claude...'` lines, NO CLIs auto-started
- I manually send-keys'd actual CLI launches — all 6 panes now running (Claude/Codex/Gemini/Kimi)
- Status-bar at bottom is tmux defaults (not the squad-branded format from launch-squad.sh) — `tmux set-option -g` calls in launch-squad.sh fail silently when run before tmux server is fully up post-kill-server
- Debugger subagent (`a7c62ac8a68b02b27`) confirmed the rollback is clean and provided diagnostic protocol for operator: `cat -v` + Ctrl-b/F-keys/Alt+digit to verify which keys actually reach tmux

## Real bugs (verified by evidence) that still need fixing

| # | Bug | Evidence | Fix |
|---|-----|----------|-----|
| 1 | `launch-squad.sh` echoes "Start with: claude..." instead of running it | Pane capture: bash prompts with echoed hints, no running CLIs after launch | Replace echoes with actual `tmux send-keys` of the CLI commands |
| 2 | `launch-squad.sh` `set-option -g` calls fail silently when server not yet up | `tmux show -g status-left` returns default format, not `"#[fg=cyan,bold]squad #[fg=white]│ "` from line 64 | Move set-option calls to AFTER first new-session, OR call `tmux start-server` first |
| 3 | Chrono drifts to direct WebFetch instead of dispatching to Leads | Operator's verbal report; no log evidence collected yet | Update `chrono/CLAUDE.md` + `chrono/SOUL.md` with explicit dispatch-first rule |

## What I should NOT do next session

- Add more keybindings, F-keys, panic keys, mouse modes, prefix highlights — Matt explicitly rejected this approach
- Auto-enable sidebar
- Build slash commands or MCP wrappers as the "fix" for tmux navigation
- Migrate to Zellij or another multiplexer
- Touch `~/.tmux.conf` until operator's terminal-key path is diagnosed via `cat -v` + screenshot

## What I SHOULD do next session

1. **Get computer-use Screen Recording permission granted** so I can take real screenshots and diagnose visually instead of theorizing
2. **Fix the 3 verified bugs above** (auto-start CLIs, set-option ordering, dispatch-first brain rule) — small targeted edits, no new architecture
3. **Run the diagnostic protocol** the debugger subagent designed:
   - `cat -v` + Ctrl-b → see if `^B` reaches tmux
   - `cat -v` + F1 → see if `^[OP` reaches tmux
   - `cat -v` + Alt+1 → see if `^[1` reaches tmux (vs `¡` glyph)
   - These produce evidence, not theory
4. **Verify each fix LIVE** in the running squad before claiming it works

## Open questions Matt raised that I haven't addressed

1. **Brain trio fix** — the new system isn't dispatching Leads. Is this Chrono brain drift OR a more systemic issue? Need to audit by sending a real task and watching dispatch behavior.
2. **chrono-claude → legacy/archive question** — should we move `~/chrono/` to legacy to prevent system-fighting confusion? **Risk:** the squad's chrono-* MCPs ARE the chrono-claude project. Moving it kills the MCPs. The naming confusion is real but the architecture coupling is real too.

## Critical context for next session

- Operator name is **Matt** (corrected mid-conversation; "Chronk" is older handle, NOT his name)
- Operator runs Apple Terminal.app on macOS; subscription billing only (Max plan, ChatGPT Plus, Gemini personal OAuth, Kimi login); env-drop pattern in launch-squad.sh
- vault: `/Users/chrono/Obsidian-Claude-Vibe-Squad/`
- chrono-claude (separate project providing MCPs): `/Users/chrono/chrono/`
- Squad runs in tmux session named `squad`
- Reattach via `tmux attach -t squad` or `bash bin/connect.sh`
- v1.1 spec: `docs/specs/2026-05-02-vibe-squad-v1.1-tool-utilization.md` (revision 2, multi-model GREEN)
- v1.1 plan: `docs/plans/2026-05-02-vibe-squad-v1.1-tool-utilization-plan.md`

## Files modified during this session (uncommitted)

- `~/.tmux.conf.v1.1-rolled-back` — preserved v1.1 config, not loaded
- Various runtime state files: `chrono/current.md`, `departments/*/memory.md`, `departments/*/current.md` (gitignored)

No uncommitted code changes pending in vault.

## Squad live state when handoff written

- Session: `squad`, 7 windows, all panes have running CLIs (auto-started manually this session)
- chrono-vault and all chrono-* MCPs: ✓ Connected
- Inbox queue: 0 across all 5 Leads
- Active tasks: 0
- Last commit: `a0110df` (rollback)

## Documentation/repo audit (2026-05-03)

**Total .md files in vault: 138** (excluding _state runtime + .git + plugin caches). Real spec/reality gaps surfaced.

### Top-level docs (all present + recent)
- `README.md`, `CHANGELOG.md`, `CONTRIBUTING.md`, `CLAUDE.md` (vault rules)

### Chrono brain (4 files — all present)
- `chrono/CLAUDE.md` — Coordinator runtime rules (updated v1.1)
- `chrono/SOUL.md` — Coordinator identity (updated v1.1 instinct-surfacing)
- `chrono/operator-setup.md` — routing rules (updated v1.1 cross-Lead direct-with-CC examples)
- `chrono/current.md` — runtime state (updated v1.1 with Cross-Lead pending replies section)

### Lead briefs (5 files — all updated v1.1)
- coding/AGENTS.md (symlinked to LEAD.md per Codex auto-load), security/CLAUDE.md, content/GEMINI.md, sysmgmt/CLAUDE.md, research/LEAD.md (no per-cwd convention for Kimi)
- Plus per-Lead: LEAD.md, memory.md, current.md

### Specialists (39 total, matches spec)
- coding: 14, security: 6, content: 6, sysmgmt: 8, research: 5

### shared/ — significant undocumented surface

**modes/ (7 files — NOT 5 as I claimed in v1.1 spec):**
- `bounty.md`, `project.md`, `research.md`, `content.md`, `incident.md`, `maintenance.md`, `triage.md`
- **Missing from spec's "5 modes" claim**: incident, maintenance, triage, content modes ALREADY EXIST as files.
- **Missing from filesystem**: no `freelance.md` mode despite v1.1 spec claiming it as one of the 5 modes. ❌ Real gap — spec says yes, reality says no.

**mode-profiles/ (16 files — ENTIRELY undocumented in v1.1 spec):**
- `mode-profiles/bounty/`: api-service.md, crypto-protocol.md, llm-app.md, mobile-app.md, smart-contract.md, web-app.md, web3.md (7 profiles)
- `mode-profiles/project/`: agent-app.md, android-app.md, backend-service.md, cli-tool.md, ios-app.md, library.md, static-site.md, web-app.md (8 profiles)
- These are per-target-type discipline docs. Spec didn't mention them at all.

**specialists/ at root of shared/ (6 cross-cutting specialists):**
- planner, prompt-engineer, skeptic, summarizer, triage, vibecoding-check
- Per spec these are "any Lead can dispatch" — but documented at `shared/specialists/` not `departments/*/specialists/`. Inconsistent location.

**protocol.md, routing.md, lifecycle.md, api-catalog.md, dispatch-toolkit.sh:**
- protocol.md: message format spec
- routing.md: routing rules
- lifecycle.md: NEW v1.1 (9 lifecycle rules)
- api-catalog.md: NEW v1.1 (verified API/feature catalog)
- dispatch-toolkit.sh: per-Lead specialist roster + MCP toolkit

### Stray .bak files (should clean up)

- `_state/capability-inventory-2026-05-02.md.bak`
- `_state/capability-inventory-2026-05-02.md.bak2`
- `bin/validate-specialists.sh.bak5`
- `shared/api-catalog.md.bak3`

These are sed `-i.bak` artifacts. Add to `.gitignore` or delete.

### Real findings — what to address next session

1. **v1.1 spec claimed 5 modes (Project/Bounty/Freelance/Research/Browse). Reality has 7 mode files (bounty/project/research/content/incident/maintenance/triage) and NO freelance.md.** 4 of my "missing" modes already exist as files; my "freelance" mode does NOT exist as a file. Significant spec/reality drift.

2. **16 mode-profile files completely undocumented in spec.** The squad has rich per-target-type discipline (api-service, crypto-protocol, llm-app, mobile-app, smart-contract, web-app, web3 for bounty; agent-app, android-app, backend-service, cli-tool, ios-app, library, static-site, web-app for project). Should be referenced in spec.

3. **Cross-cutting specialists location inconsistent.** `shared/specialists/` has 6 cross-cutting; `departments/*/specialists/` has 39 lead-scoped. Maybe rename `shared/specialists/` → `cross-cutting/` for clarity?

4. **Lead memory.md files all have v1.1 update notes** (committed `ee3f3e5`) — verified.

5. **Specialist files all have v1.1 schema** (committed `5797ced`, validated by `bin/validate-specialists.sh` — 39/39 PASS).

### Recommended audit work for next session

1. **Sync spec to reality** — add the 16 mode-profile files + 4 unaccounted modes (incident/maintenance/triage/content) to spec; remove freelance OR create freelance.md.
2. **Clean .bak files** — `find . -name "*.bak*" -not -path "./.git/*" -delete`
3. **Decide cross-cutting specialists location** — `shared/specialists/` vs new `cross-cutting/` directory.
4. **Verify all .md files are accurate post-v1.1** — especially routing.md and protocol.md (touched by v1.1 changes? need diff vs pre-v1.1).
5. **README.md verification** — read through to ensure v1.1 highlights match shipped reality.

## Recommended first action next session

1. Confirm Screen Recording permission granted (`mcp__computer-use__request_access` should succeed)
2. Take screenshot to see operator's actual terminal state
3. Walk operator through the 3-step diagnostic protocol if Ctrl-b is still flaky
4. Fix the 3 verified bugs (auto-start CLIs, set-option ordering, dispatch-first brain) ONE AT A TIME with verification between
5. NO architectural changes, NO new keybindings, NO new layers
