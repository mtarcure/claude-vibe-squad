{
  "name": "chrono-dedup",
  "version": "0.1.0",
  "description": "Target-scoped prior-art checks for findings and composite attack chains.",
  "license": "AGPL-3.0-or-later",
  "mcpServers": {
    "chrono-dedup": {
      "command": "${CLAUDE_PLUGIN_ROOT}/../../.venv/bin/python",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp_server.py"]
    }
  }
}
