# TASK-2026-08-04-1300-mmrinv2 plan

1. Lock the exact read-only target allowlist and pins; recall durable context once; read the cited incident root cause.
2. Define five falsifiable source obligations and a conservative status model that cannot call an empty census clean.
3. Implement Solidity and Rust/Anchor canonical-form detection, plus Solidity Semgrep evidence and Rust positive-controlled `rg` fallback.
4. Build one vulnerable/fixed pair per invariant per language and a runtime CalculateRoot regression with a valid negative control.
5. Compile and execute all controls; apply the bounded check to project-owned production source and real vendored verifiers at the pinned commits.
6. Manually classify guards, call-site ownership, limitations, no-self-inflicted evidence, and the primitive ledger.
7. Hash the artifact set, record durable memory once, request the mandated opposite-family review, and deliver locally without submission.
