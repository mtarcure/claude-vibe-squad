# Primitive ledger

This is a calibration ledger, not a target finding ledger. No target was audited and no row is promoted to exploitability, severity, CVSS, or submission status.

| ID | Bounded capability/observation | Preconditions | State changed | Evidence | Inert alone? | Disposition |
|---|---|---|---|---|---|---|
| P-01 | An out-of-range leaf can remain outside a one-peak reconstruction while a supplied proof element becomes the returned root in the vulnerable CalculateRoot shape. | `leafCount=1`, one `index=1` leaf, proof containing accepted root; missing I1 and I2. | None in the pure verifier model. | `tests/CalculateRootRegression.t.sol`; three Forge controls pass. | No for root substitution; downstream impact is deliberately not modeled. | Known incident-class regression control, not a new finding. |
| P-02 | A lexical guard can be present without proving acceptance-path dominance or root-linked consumption. | Dead branch, dummy cursor, ignored helper result, shadowing, or guard after return. | None. | Detector-design challenge; checker emits `present_signal`, never PASS. | Yes. | Kept as a detector limitation and review requirement. |
| P-03 | Chainlink `MerkleMultiProof.merkleRoot` closes the real leaf and proof cursors in one terminal conjunction. | Non-empty, bounded multiproof reaches loop completion. | None; pure root reconstruction. | `MerkleMultiProof.sol:66-96`. | Yes. | I2 and I4 supported locally; I1/I3 have no index API; I5 not local. |
| P-04 | OpenZeppelin v5.1 multiproof enforces a shape equation and terminal proof exhaustion; together with queue semantics, accepted non-empty paths consume all leaves. | Supported complete-tree multiproof and accepted execution. | None; pure root reconstruction. | `MerkleProof.sol:212-242`. | Yes. | I2 supported by manual derivation, I4 explicit; source checker conservatively leaves I2 as a review hit. |
| P-05 | OpenZeppelin v4.8 multiproof has the shape equation but no terminal proof-cursor check. | Multiproof variant; single-proof functions are separate. | None; pure root reconstruction. | v4.8 `MerkleProof.sol:131-160`. | Yes. | I2 supported by queue/shape reasoning; I4 has no local terminal guard signal. This is not asserted exploitable. |
| P-06 | Chainlink and OpenZeppelin default pair hashing is commutative/sorted. | Default hasher. | Loses left/right orientation in the library commitment. | Chainlink `MerkleMultiProof.sol:108-111`; OZ `Hashes.sol:17-18`. | Yes. | I5 must be supplied by authenticated leaf content/application semantics; sequence-number content is not absolute tree-position binding. |
| P-07 | The project-owned EVM source has no qualifying proof/Merkle/MMR verifier surface at the pinned commit. | Source-only census of 21 production Solidity files. | None. | `results/evm-project.json`; positive dependency controls found. | Yes. | BOUNDED absence of surface, not clean target or exploitability verdict. |
| P-08 | The project-owned Rust/Anchor programs have no qualifying proof/Merkle/MMR verifier surface at the pinned commit. | Source-only census of 19 tracked program `.rs` files. | None. | `results/svm-project.json`; positive-controlled `rust-field-census.sh`. | Yes. | BOUNDED absence of surface, not clean target or exploitability verdict. |

## Re-walk for chaining

The packet explicitly says “Free-mode build. No target is audited.” Therefore no attacker-controlled target head, target state transition, or payout terminus was authorized or modeled. P-03 through P-08 are library/census observations and do not establish a target chain. P-01 terminates at pure-root substitution in a sandbox model; downstream application impact belongs to the cited historical incident, not this task. No row was deleted or killed as “not exploitable.”
